bool isValid(const char *szInput)
{
    std::stack<char> stk;
    if (szInput == nullptr)
        return false;
    char ch = *szInput;
    while(ch!='/n'){
        if(ch=='(')
            stk.push_back()
    }
}